{% extends 'base.html.twig' %}

{% block title %}Ajouter une catégorie !{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-uppercase fs-3 mb-4">{{ controller_name }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form.name) }}

    <button type="submit" class="btn btn-sm btn-success mt-3">Enregistrer</button>
    {{ form_end(form) }}
</div>
{% endblock %}